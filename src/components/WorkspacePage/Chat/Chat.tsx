import { Flex, Layout, Splitter } from "antd"
import { observer } from "mobx-react-lite"


export const Chat = observer(() => {
return(
        <Flex id='container' className='w-full h-[100%]' style={{position: 'relative'}}>
df
        </Flex>
)})